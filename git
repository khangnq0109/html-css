git init                             khởi tạo
git remote add origin link           tạo liên kết giữa git cá nhân và git online
 2 câu lệnh trên chỉ chạy 1 lần



git add .                           đóng gói code
git commit -m 'sửa lỗi'             so sánh với lịch sử gần nhất và lưu sự thay đổi vào lịch sử  
git push origin tên nhánh           đẩy lịch sử lên git online

git quản lí lịch sử ( các mốc commit)

git clone                           git init + git remote add + lấy toàn bộ lịch sử
git pull                            lấy lịch sử mới nhất trên git online về
luôn pull trước khi push